# OPSEC Input Data (Ignored)

This directory contains raw DNS query data used as input
for the analysis pipeline.

Expected file:
- dns_queries.txt (one domain per line)

⚠️ All files except this README are ignored by git.
Never commit real DNS data.
